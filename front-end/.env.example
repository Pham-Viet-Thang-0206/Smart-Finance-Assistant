# Native app:
# Leave this empty to let Expo infer the current dev host automatically.
# Set it explicitly only when using a real device through LAN/ngrok.
EXPO_PUBLIC_API_BASE_URL=

# Web app runs in the local browser on the same machine as the backend.
EXPO_PUBLIC_API_BASE_URL_WEB=http://localhost:4000
